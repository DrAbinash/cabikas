# Local runtime data (settings + uploaded photo); in production this lives
# in the bikas-ca-data Docker volume.
data/

# Local environment configuration — copy .env.example to .env and edit.
.env
.env.*
!.env.example

*.log
.DS_Store
